# Current Task: Day 1 - Backend Core Foundation

## 🎯 My Goals for Today (User)
- [x] **Database Connection:** Create `backend/src/utils/db.js` to handle Mongoose connection to MongoDB.
- [x] **Environment Setup:** Create a `.env.example` file with required variables (PORT, MONGO_URI, JWT_SECRET).
- [x] **Error Handling:** Implement a custom `ErrorResponse` class and a global `errorHandler` middleware.
- [x] **App Refinement:** Integrate the database connection and global error handler into `backend/src/app.js`.

## 👩‍💻 Radhika's Goals (Teammate)
- [ ] **User Model:** Define the User schema with roles (coach, player, etc.).
- [ ] **Team Model:** Define the Team schema.
- [ ] **Player Model:** Define the Player profile schema.

---

## ✅ Completed
- [x] Project Vision & Tech Stack defined in `GEMINI.MD`
- [x] Initial Directory Structure created
- [x] API Specifications documented in `API-SPECS.MD`
- [x] 10-Day Sprint Plan created
- [x] Backend converted to Vanilla JavaScript
- [x] Basic `app.js` entry point created
- [x] NPM dependencies and `package.json` configured
- [x] Root `.gitignore` and `backend/.env.example` created
